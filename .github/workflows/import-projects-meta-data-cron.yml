name: import-projects-meta-data-cron
on:
  schedule:
    - cron: '*/5 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: hit "import-projects-meta-data-route" every 5 minutes cron job
        run: |
          curl --request POST \
          --url 'https://www.david-kimmich-portfolio.de/api/projects/import/meta-data' \
          --header 'Authorization: Bearer ${{ secrets.IMPORT_PROJECTS_CRON_ACCESS_TOKEN }}'