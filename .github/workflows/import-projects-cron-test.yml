name: import-projects-cron-test
on:
    push:
        branches:
            - "test-workflows"
jobs:
    cron:
        runs-on: ubuntu-latest
        steps:
            - name: get auth token
              run: |
                  echo ${{secrets.ADMIN_USERNAME}}
                    authToken=$(curl --request POST --url --data '{"username": "${{ secrets.ADMIN_USERNAME }}", "password": "${{ secrets.ADMIN_PASSWORD }}"}' '${{ vars.API_URL }}/auth/custom-sign-in')
                    echo $authToken
            - name: hit "import-projects-route"
              run: |
                  echo $authToken
                    curl --request POST \
                    --url '${{ vars.API_URL }}${{ vars.IMPORT_PROJECTS_ROUTE }}' \
                    --header 'auth-token: $authToken'
